# ------------------------------------
# Matias Finochio - 2026 - MIT License
# ------------------------------------
{.push raises: [].}
import types, vec2, rect2, color, transform2, scene, gamemacro, backend, renderer, input, texture, camera
export types, vec2, rect2, color, transform2, scene, gamemacro, backend, renderer, input, texture, camera
{.pop.}
